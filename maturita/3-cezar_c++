#include <iostream>
char inp[] = "Hello";
int posun = 4;

int main(){
    for (int i = 0; i < sizeof(inp); i++){

        if (inp[i] > 96 && inp[i] < 123){
            //male
        }
        else if (inp[i] > 64 && inp[i] < 91){
            //velke
        }
        else{

        }


    }
}
